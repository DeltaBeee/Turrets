package net.reaper.reaperturrets.world;

import net.minecraft.data.worldgen.BootstapContext;
import net.minecraft.world.damagesource.DamageType;

public class DDamageTypes {
    //public static final ResourceKey<DamageType> EXAMPLE = ResourceKey.create(Registries.DAMAGE_TYPE, new ResourceLocation(ExampleProject.MOD_ID, "example"));

    public static void bootstrap(BootstapContext<DamageType> context) {
        //context.register(EXAMPLE, new DamageType("example", 0.1F));
    }
}
